// Environment variables with defaults for development
export const env = {
  // Admin credentials
  ADMIN_PASSWORD: process.env.KULLANICI_SIFRE || 'admin123',
  
  // Add other environment variables here as needed
}; 